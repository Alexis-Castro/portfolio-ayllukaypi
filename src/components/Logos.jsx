import React from 'react'

export const Logos = ({className}) => {
   return (
      <>
         <div className={`absolute h-full bg-repeat-y bg-center bg-logo-page w-32 brightness-100 top-0 ${className}`}>
         </div>
      </>
   )
}
