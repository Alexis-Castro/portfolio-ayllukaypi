
export const Corte = ({className = ""}) => {
   return (
      <>
         <div className={`absolute h-10 bg-no-repeat bg-center left-0 right-0 w-full bottom-3 bg-corte-page ${className}`}>
         </div>
      </>
   )
}
